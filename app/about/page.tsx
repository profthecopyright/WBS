import type { Metadata } from "next";
import { AboutPage } from "../site-pages";
export const metadata: Metadata = {title: "About WBS | World Bridge Services", description: "Who WBS is, what the bridge agency does, and how Brian Glubok and his colleagues built it."};
export default AboutPage;
